{
  "name": "brreg-mcp-server",
  "version": "1.0.0",
  "description": "MCP server for Norwegian Business Registry API",
  "main": "dist/brreg-mcp-server.js",
  "scripts": {
    "build": "tsc",
    "start": "node dist/brreg-mcp-server.js",
    "dev": "tsc && node dist/brreg-mcp-server.js"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^0.5.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.0.0"
  }
}